<Page
    x:Class="XboxControllerTester.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XboxControllerTester"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Loaded="Page_Loaded"
    Unloaded="Page_Unloaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Thanh công cụ (đã chặn focus bằng tay cầm trong code-behind) -->
        <CommandBar x:Name="TopBar" Grid.Row="0" DefaultLabelPosition="Right">
            <AppBarButton x:Name="btnReset"
                          Icon="Refresh"
                          Label="Reset đếm"
                          Click="ResetCounts_Click"/>
        </CommandBar>

        <!-- Khung & vùng hiển thị -->
        <Border x:Name="gridBorder"
                Grid.Row="1"
                Margin="8"
                CornerRadius="12"
                BorderThickness="4"
                BorderBrush="Transparent">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <Grid x:Name="gridStatus" Margin="8"/>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
